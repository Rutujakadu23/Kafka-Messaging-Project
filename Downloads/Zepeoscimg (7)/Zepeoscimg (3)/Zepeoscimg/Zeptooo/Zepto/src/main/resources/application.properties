# Spring Application Name
spring.application.name=Zepto

# MySQL Database Configuration (CORRECTED)
spring.datasource.url=jdbc:mysql://65.0.19.232:3306/pr_pr1?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&autoReconnect=true
spring.datasource.username=root  # Changed from root
spring.datasource.password=Zapeo@Scrimatec#2025

# Connection pool settings (ADD THESE)
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.show_sql=true
spring.jpa.open-in-view=true
spring.main.allow-bean-definition-overriding=true

# Static Resources Configuration
spring.mvc.static-path-pattern=/uploads/**                              
spring.web.resources.static-locations=file:/C:/uploads/

# Thymeleaf Configuration
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.enabled=true
spring.thymeleaf.check-template-location=false
spring.jackson.serialization.FAIL_ON_EMPTY_BEANS=false


# JSON Configuration
spring.jackson.serialization.indent-output=true
spring.jackson.default-property-inclusion=non-null

# Razorpay Configuration
razorpay.key=rzp_test_MnTt8kBzkg8yil
razorpay.secret=gK9kQvBn9IfXRf7V7G4cclap

# Multipart File Upload Configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Logging Configuration
logging.level.org.springframework.web=DEBUG

# Server Port Configuration
server.port=8080
